services:
  - type: web
    name: job-scraper-demo
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd app && gunicorn main:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3
